name: lista_de_tareas
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  cupertino_icons: ^1.0.8
  test: ^1.24.0
  mockito: ^5.4.0
  build_runner: ^2.4.0
  provider: ^6.1.4
  http: ^1.1.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
